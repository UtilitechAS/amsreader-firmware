/**
 * Custom baud/parity list for meter autodetection.
 *
 * When this file is present as custom/baudrates.h it replaces the default
 * AUTO_BAUD_RATES[] array defined in PassiveMeterCommunicator.cpp.
 *
 * Uncomment and adjust the entries below to match your target meters.
 * Entries are tried in order during autodetection.
 */

// const uint32_t AUTO_BAUD_RATES[] = { 2400, 9600, 115200 };
// const uint8_t  AUTO_BAUD_RATES_COUNT = sizeof(AUTO_BAUD_RATES) / sizeof(AUTO_BAUD_RATES[0]);
